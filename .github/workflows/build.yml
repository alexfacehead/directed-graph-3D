name: Build

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            name: macOS
            artifact: directed_graph-macos
          - os: ubuntu-latest
            name: Linux
            artifact: directed_graph-linux
          - os: windows-latest
            name: Windows
            artifact: directed_graph-windows

    runs-on: ${{ matrix.os }}
    name: ${{ matrix.name }}

    steps:
      - uses: actions/checkout@v4

      # Linux: install OpenGL + X11 dev headers
      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1-mesa-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev libxext-dev libwayland-dev libxkbcommon-dev

      - name: Configure
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release -j4

      # macOS: create .app bundle with launcher script + .dmg
      - name: Package (macOS)
        if: runner.os == 'macOS'
        run: |
          APP="dist/Hypergraph3D.app"
          mkdir -p "$APP/Contents/MacOS"
          mkdir -p "$APP/Contents/Resources"

          # Put the binary in Resources (not as the direct executable)
          cp build/directed_graph "$APP/Contents/Resources/directed_graph_bin"
          chmod +x "$APP/Contents/Resources/directed_graph_bin"

          # Create a launcher script as the bundle executable
          # Gatekeeper is lenient with scripts, strict with Mach-O binaries
          cat > "$APP/Contents/MacOS/Hypergraph3D" << 'LAUNCHER'
#!/bin/bash
DIR="$(cd "$(dirname "$0")/../Resources" && pwd)"
xattr -cr "$DIR/directed_graph_bin" 2>/dev/null
exec "$DIR/directed_graph_bin"
LAUNCHER
          chmod +x "$APP/Contents/MacOS/Hypergraph3D"

          cp packaging/macos/Info.plist "$APP/Contents/Info.plist"

          # Create .dmg with Applications symlink for drag-to-install
          mkdir -p dist/dmg_staging
          cp -R "$APP" dist/dmg_staging/
          ln -s /Applications dist/dmg_staging/Applications
          hdiutil create -volname "Hypergraph3D" -srcfolder dist/dmg_staging -ov -format UDZO dist/Hypergraph3D.dmg

      - name: Package (Linux)
        if: runner.os == 'Linux' && matrix.name == 'Linux'
        run: |
          mkdir -p dist
          cp build/directed_graph dist/
          chmod +x dist/directed_graph

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          mkdir -p dist
          cp build/Release/directed_graph.exe dist/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/

  # Create a GitHub Release when a tag is pushed
  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Package release archives
        run: |
          cd artifacts
          # macOS: .dmg is already built, just move it
          if [ -f directed_graph-macos/Hypergraph3D.dmg ]; then
            mv directed_graph-macos/Hypergraph3D.dmg ./Hypergraph3D.dmg
          fi
          # Linux: tar.gz
          if [ -d directed_graph-linux ]; then
            tar czf directed_graph-linux.tar.gz -C directed_graph-linux .
          fi
          # Windows: zip
          if [ -d directed_graph-windows ]; then
            (cd directed_graph-windows && zip -r ../directed_graph-windows.zip .)
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/Hypergraph3D.dmg
            artifacts/directed_graph-linux.tar.gz
            artifacts/directed_graph-windows.zip
